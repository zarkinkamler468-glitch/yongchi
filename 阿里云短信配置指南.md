# 阿里云短信配置指南

系统支持在超管后台选择“腾讯云短信”或“阿里云短信”。两套配置分别保存，切换服务商不会删除另一套配置。

## 一、阿里云控制台准备

1. 使用企业实名认证主体开通阿里云短信服务。
2. 在“国内消息 → 资质管理”提交企业资质。
3. 在“签名管理”申请与营业执照、商标或小程序主体一致的短信签名。
4. 在“模板管理”申请通知短信模板。系统固定传入三个变量：

   | 变量 | 内容 | 示例 |
   | --- | --- | --- |
   | `${name}` | 会员姓名 | 张伟 |
   | `${type}` | 业务类型 | 储值充值 |
   | `${detail}` | 账户变动说明 | 充值500元 |

   可尝试模板：

   ```text
   尊敬的${name}，您的会员账户发生${type}变动，变动说明：${detail}。如非本人操作，请联系门店。
   ```

   阿里云实际审核结果为准。若审核认为 `detail` 范围过大，建议分别申请充值、核销、退款模板，或使用无变量固定通知模板；不要把通知短信伪装成验证码。
5. 模板审核通过后记录模板 CODE，格式通常为 `SMS_123456789`。
6. 在 RAM 访问控制中创建专用用户，只授予短信发送权限，创建并保存 AccessKey ID 和 AccessKey Secret。不要使用主账号密钥，也不要把密钥提交到 Git。

## 二、系统后台配置

1. 使用超管账号进入“短信通知”。
2. 服务商选择“阿里云短信”。
3. 填写 AccessKey ID、AccessKey Secret、短信签名和模板 CODE。
4. 先保存配置，再填写测试手机号发送测试短信。
5. 测试成功后勾选“启用会员账户变动短信”并保存。

密钥保存后不会回显；留空表示保留原密钥。切换回腾讯云时，阿里云配置仍会保留。

## 三、常见错误

- `InvalidAccessKeyId.NotFound`：AccessKey ID 不存在或填写错误。
- `SignatureDoesNotMatch`：AccessKey Secret 错误或签名计算失败。
- `isv.SMS_SIGNATURE_ILLEGAL`：短信签名不存在、未审核或填写不一致。
- `isv.SMS_TEMPLATE_ILLEGAL`：模板 CODE 不存在或未审核。
- `isv.TEMPLATE_MISSING_PARAMETERS`：模板变量不是 `name`、`type`、`detail`，或变量数量不匹配。
- `isv.BUSINESS_LIMIT_CONTROL`：触发阿里云发送频率限制。
- `isv.AMOUNT_NOT_ENOUGH`：短信余额不足。

生产服务器需要能够访问 `dysmsapi.aliyuncs.com:443`。
