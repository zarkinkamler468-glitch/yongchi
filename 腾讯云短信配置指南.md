# 腾讯云短信配置指南

本系统支持腾讯云和阿里云两种短信服务商。本指南说明腾讯云配置；如腾讯云账号不允许通知模板使用变量，可在超管后台切换阿里云，并参阅《阿里云短信配置指南.md》。短信发送失败只会在服务器日志中记录，不会影响收银、退款或核销。

## 一、腾讯云控制台准备

1. 使用企业主体登录腾讯云控制台，并完成实名认证。
2. 开通“腾讯云短信（SMS）”服务。在控制台搜索“短信”，进入后按指引开通；中国大陆短信通常需要完成资质和签名审核后才能正式发送。
3. 进入“国内短信 > 签名管理”，创建签名。例如门店主体为“峡谷游泳馆”，可申请签名“峡谷游泳馆”。签名必须与营业执照、商标授权或其他资质一致，等待审核通过。
4. 进入“国内短信 > 正文模板管理”，创建通知类模板。如果当前腾讯云账号允许通知模板使用变量，可尝试：

   ```text
   {1}，您的会员账户{2}，{3}。如非本人操作请联系门店。
   ```

   系统会按如下顺序填充变量：

   | 模板变量 | 系统填充内容 | 示例 |
   | --- | --- | --- |
   | `{1}` | 会员姓名 | 张伟 |
   | `{2}` | 业务类型 | 储值充值 / 入场核销 |
   | `{3}` | 账户变动说明 | 充值500元 / 扣减1次，剩余9次 |

   模板审核通过后，记录模板 ID。不要自行调整变量的顺序或数量，否则腾讯云会拒绝发送。如果控制台明确提示“通知短信不支持传入变量”，不要改选验证码短信规避审核；可以改用阿里云通知模板，或申请无变量固定通知模板并另行调整系统模板模式。
5. 进入“访问管理（CAM）> 用户 > 用户列表”，建议创建一个专用于本系统的子账号；为该子账号授予腾讯云短信发送所需权限（可使用预设的短信全读写权限，或按企业安全规范创建仅含 `sms:SendSms` 的最小权限策略）。
6. 在子账号的“API 密钥”中创建并保存 `SecretId` 与 `SecretKey`。`SecretKey` 只在创建时完整显示一次，务必安全保存，切勿发送到聊天工具、截图或提交到代码仓库。
7. 回到短信控制台首页，找到“应用管理”或“应用列表”，记录“短信 SdkAppId”（通常形如 `1400xxxxxx`）。

## 二、在本系统中填写

1. 用**超管**账号登录电脑后台。老板、前台、财务账号不会看到此页面，也无法调用短信配置接口。
2. 左侧进入“短信通知”。
3. 按腾讯云控制台的资料填写：
   - 启用会员账户变动短信
   - SecretId
   - SecretKey
   - 短信 SdkAppId
   - 已审核的短信签名（只填签名文字，不加【】）
   - 已审核的账户变动模板 ID
4. 点击“保存短信配置”。SecretId、SecretKey 保存后不会回显；需要更换时重新填写即可，留空不会清除原密钥。
5. 填写一位可接收短信的中国大陆手机号，点击“发送测试短信”。收到测试短信后再正式启用业务通知。

## 三、排错清单

- `UnauthorizedOperation`：SecretId/SecretKey 无效，或 CAM 子账号没有短信发送权限。
- `FailedOperation.SignatureIncorrectOrUnapproved`：签名未审核通过，或填写的签名与审核结果不一致。
- `FailedOperation.TemplateIncorrectOrUnapproved`：模板 ID 不正确、未审核，或模板变量数量/顺序与系统约定不同。
- `LimitExceeded` / `FailedOperation.FrequencyLimit`：命中腾讯云频率或日发送限制，请在短信控制台检查套餐、频率限制和发送记录。
- 手机号无效：系统只会向中国大陆 11 位会员手机号发送；请在会员资料中保存正确号码。
- 未收到短信：检查模板是否审核通过、短信余额/套餐、控制台发送记录，以及手机拦截设置。

## 四、上线安全建议

- 生产环境使用专用 CAM 子账号，不使用腾讯云主账号密钥。
- 定期轮换 SecretKey；更换后先在本系统保存，再发送测试短信。
- 只使用已获得会员授权的通知用途，遵守短信签名、模板及个人信息保护要求。
- 短信内容不应包含身份证号、完整银行卡号、密码或验证码等敏感信息。
- 服务器必须能通过 HTTPS 访问 `sms.tencentcloudapi.com:443`。若部署环境有限制外网访问，请由运维放通该地址。
